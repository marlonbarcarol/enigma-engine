{
	// This file is a specific configuration for supporting development tools
	// Ideally it would be located at the root directory with a different name such as "tsconfig.tools.json"
	// However, code editors like vscode does not support having different tsconfig namings.

	"extends": "../tsconfig.json",

	"compilerOptions": {
		"noEmit": true
	},

	"include": [
		"./**/*.ts",
		"../.eslintrc.*",
		"../jest.config.js"
	],
	"exclude": ["node_modules", "build"]
}
