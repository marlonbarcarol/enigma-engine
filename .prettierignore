.cache
.vscode
workspace.code-workspace

build
package-lock.json
node_modules

CHANGELOG.md

# Planning artifacts contain code fragments deliberately indented to match
# their eventual position in a real file (not full standalone units) --
# Prettier's embedded-code formatting would flatten that indentation and
# corrupt the fragments.
docs/superpowers/

# demo/ is an independent subproject with its own toolchain (lint, format,
# test), fully decoupled from the engine's own -- see
# docs/superpowers/specs/2026-09-04-enigma-visualizer-design.md.
demo/
